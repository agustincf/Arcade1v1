[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Variables de entorno usadas por los scripts de despliegue:
#   BASE_SEPOLIA_RPC_URL, PRIVATE_KEY, USDC_ADDRESS, ARBITER_ADDRESS,
#   PLATFORM_WALLET, FEE_BPS
