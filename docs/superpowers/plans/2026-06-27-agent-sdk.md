# agent-sdk Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir `@arcade1v1/agent-sdk`, un paquete portable que permite escribir un agente que juega y compite por ranking en Arcade1v1 en menos de 20 líneas.

**Architecture:** Paquete nuevo en `packages/agent-sdk` con cuatro piezas chicas y enfocadas: cliente HTTP del árbitro (`client.ts`), firma del envío + wallet efímera (`sign.ts`), estrategias de juego (`strategies.ts`), y los helpers de alto nivel (`agent.ts`). Reutiliza los motores y la firma canónica de `@arcade1v1/game-sdk`. No toca el contrato ni el árbitro.

**Tech Stack:** TypeScript (ESM), `viem` (wallet + firma), `@arcade1v1/game-sdk` (motores + `scoreAuthMessage`), `node:test` + `tsx` para los tests (mismo patrón que `game-sdk`).

## Global Constraints

- Paquete ESM: `"type": "module"` en `package.json`. Imports con extensión `.js`.
- Nombre exacto del paquete: `@arcade1v1/agent-sdk`, `"version": "0.0.0"`, `"private": true`.
- Solo Fase 1 (por ranking): NADA de on-chain (sin depósitos, sin `settle`, sin USDC). La wallet es solo para **firmar** el envío.
- No modificar `packages/contracts/**` ni `apps/server/**`.
- Tests con `node --import tsx --test` (igual que `game-sdk`); sin frameworks extra.
- Reutilizar `@arcade1v1/game-sdk/auth` (`scoreAuthMessage`) y `@arcade1v1/game-sdk/g2048` (`Game2048`) — no reimplementar.
- TypeScript estricto (hereda el `tsconfig` base del repo).

---

### Task 1: ArbiterClient + scaffold del paquete

**Files:**
- Create: `packages/agent-sdk/package.json`
- Create: `packages/agent-sdk/tsconfig.json`
- Create: `packages/agent-sdk/src/client.ts`
- Create: `packages/agent-sdk/src/index.ts`
- Test: `packages/agent-sdk/test/client.test.ts`
- Modify: `package.json` (root) — ampliar el glob de `test` para incluir el nuevo paquete

**Interfaces:**
- Produces:
  - `interface MatchView { matchId: string; game: string; stake: number; seed: number; status: "waiting"|"ready"|"settled"|"draw"; role?: "p1"|"p2"; opponent?: string; scores: Record<string, number>; outcome?: "p1"|"p2"|"draw"; winner?: string; signature?: string; yourScore?: number; rivalScore?: number; margin?: number; netPnl?: number; rivalReplay?: unknown; rating?: number; ratingDelta?: number; }`
  - `class ArbiterClient { constructor(baseUrl: string); matchmake(game: string, stake: number, address: string): Promise<MatchView>; submitScore(id: string, address: string, score: number, replay?: unknown, signature?: string): Promise<MatchView>; getMatch(id: string, address?: string): Promise<MatchView>; leaderboard(game: string, limit?: number): Promise<{ address: string; rating: number }[]>; rating(address: string): Promise<Record<string, number>>; }`

- [ ] **Step 1: Create the package scaffold**

`packages/agent-sdk/package.json`:
```json
{
  "name": "@arcade1v1/agent-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./sign": "./src/sign.ts",
    "./strategies": "./src/strategies.ts"
  },
  "dependencies": {
    "@arcade1v1/game-sdk": "*",
    "viem": "^2.53.1"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
```

`packages/agent-sdk/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src", "test", "examples"]
}
```
(Si no existe `tsconfig.base.json` en la raíz, copiar el `compilerOptions` de `packages/game-sdk/tsconfig.json` si lo tuviera; si `game-sdk` no tiene tsconfig propio, usar `{ "compilerOptions": { "module": "NodeNext", "moduleResolution": "NodeNext", "strict": true, "target": "ES2022", "noEmit": true }, "include": ["src", "test", "examples"] }`.)

- [ ] **Step 2: Write the failing test**

`packages/agent-sdk/test/client.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { ArbiterClient } from "../src/client.ts";

function fakeFetch(captured: { url?: string; init?: RequestInit }, body: unknown) {
  return (async (url: string, init?: RequestInit) => {
    captured.url = String(url);
    captured.init = init;
    return new Response(JSON.stringify(body), { status: 200, headers: { "Content-Type": "application/json" } });
  }) as typeof fetch;
}

test("matchmake hace POST /matchmake con el body correcto", async () => {
  const cap: { url?: string; init?: RequestInit } = {};
  const client = new ArbiterClient("http://arbiter.test", { fetchImpl: fakeFetch(cap, { matchId: "0xabc", game: "2048", stake: 5, seed: 42, status: "waiting", scores: {} }) });
  const m = await client.matchmake("2048", 5, "0xPLAYER");
  assert.equal(cap.url, "http://arbiter.test/matchmake");
  assert.equal(cap.init?.method, "POST");
  assert.deepEqual(JSON.parse(String(cap.init?.body)), { game: "2048", stake: 5, address: "0xPLAYER" });
  assert.equal(m.matchId, "0xabc");
  assert.equal(m.seed, 42);
});

test("leaderboard hace GET y devuelve el top", async () => {
  const cap: { url?: string; init?: RequestInit } = {};
  const client = new ArbiterClient("http://arbiter.test", { fetchImpl: fakeFetch(cap, { game: "2048", top: [{ address: "0x1", rating: 1200 }] }) });
  const top = await client.leaderboard("2048", 10);
  assert.equal(cap.url, "http://arbiter.test/leaderboard/2048?limit=10");
  assert.deepEqual(top, [{ address: "0x1", rating: 1200 }]);
});
```

- [ ] **Step 3: Run the test to verify it fails**

Run: `node --import tsx --test packages/agent-sdk/test/client.test.ts`
Expected: FAIL — no se puede importar `ArbiterClient` (módulo `../src/client.ts` no existe).

- [ ] **Step 4: Write the implementation**

`packages/agent-sdk/src/client.ts`:
```ts
// Cliente HTTP portable del árbitro de Arcade1v1 (sin Next.js: sirve en Node,
// navegador, agentes). Inyectable: se le puede pasar un fetch propio para tests.

export interface MatchView {
  matchId: string;
  game: string;
  stake: number;
  seed: number;
  status: "waiting" | "ready" | "settled" | "draw";
  role?: "p1" | "p2";
  opponent?: string;
  scores: Record<string, number>;
  outcome?: "p1" | "p2" | "draw";
  winner?: string;
  signature?: string;
  yourScore?: number;
  rivalScore?: number;
  margin?: number;
  netPnl?: number;
  rivalReplay?: unknown;
  rating?: number;
  ratingDelta?: number;
}

export interface LeaderRow {
  address: string;
  rating: number;
}

export interface ArbiterClientOptions {
  fetchImpl?: typeof fetch;
}

export class ArbiterClient {
  private base: string;
  private fetchImpl: typeof fetch;

  constructor(baseUrl: string, opts: ArbiterClientOptions = {}) {
    this.base = baseUrl.replace(/\/$/, "");
    this.fetchImpl = opts.fetchImpl ?? fetch;
  }

  private async post(path: string, body: unknown): Promise<MatchView> {
    const r = await this.fetchImpl(`${this.base}${path}`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    if (!r.ok) throw new Error(`arbiter ${path} ${r.status}: ${await r.text()}`);
    return (await r.json()) as MatchView;
  }

  matchmake(game: string, stake: number, address: string): Promise<MatchView> {
    return this.post("/matchmake", { game, stake, address });
  }

  submitScore(id: string, address: string, score: number, replay?: unknown, signature?: string): Promise<MatchView> {
    return this.post(`/match/${id}/score`, { address, score, replay, signature });
  }

  async getMatch(id: string, address?: string): Promise<MatchView> {
    const q = address ? `?address=${address}` : "";
    const r = await this.fetchImpl(`${this.base}/match/${id}${q}`);
    if (!r.ok) throw new Error(`arbiter get ${r.status}`);
    return (await r.json()) as MatchView;
  }

  async leaderboard(game: string, limit = 20): Promise<LeaderRow[]> {
    const r = await this.fetchImpl(`${this.base}/leaderboard/${game}?limit=${limit}`);
    if (!r.ok) throw new Error(`arbiter leaderboard ${r.status}`);
    const j = (await r.json()) as { top?: LeaderRow[] };
    return j.top ?? [];
  }

  async rating(address: string): Promise<Record<string, number>> {
    const r = await this.fetchImpl(`${this.base}/rating/${address}`);
    if (!r.ok) throw new Error(`arbiter rating ${r.status}`);
    const j = (await r.json()) as { ratings?: Record<string, number> };
    return j.ratings ?? {};
  }
}
```

`packages/agent-sdk/src/index.ts`:
```ts
export { ArbiterClient } from "./client.js";
export type { MatchView, LeaderRow } from "./client.js";
```

- [ ] **Step 5: Run the test to verify it passes**

Run: `node --import tsx --test packages/agent-sdk/test/client.test.ts`
Expected: PASS — ambos tests pasan, salida limpia.

- [ ] **Step 6: Wire the new package into the root test script**

In `package.json` (root), change the `test` script from:
```
"test": "node --import tsx --test packages/game-sdk/test/*.test.ts",
```
to:
```
"test": "node --import tsx --test packages/*/test/*.test.ts",
```

Run: `npm test`
Expected: PASS — corren los tests de `game-sdk` y `agent-sdk`, todos verdes.

- [ ] **Step 7: Commit**

```bash
git add packages/agent-sdk/package.json packages/agent-sdk/tsconfig.json packages/agent-sdk/src/client.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/client.test.ts package.json
git commit -m "feat(agent-sdk): ArbiterClient portable + scaffold del paquete"
```

---

### Task 2: Firma del envío + wallet efímera

**Files:**
- Create: `packages/agent-sdk/src/sign.ts`
- Test: `packages/agent-sdk/test/sign.test.ts`

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces:
  - `function randomWallet(): { privateKey: Hex; address: Hex }`
  - `function signScore(opts: { matchId: string; address: string; score: number; privateKey: Hex }): Promise<Hex>`
  - (`Hex` es el tipo de `viem`.)

- [ ] **Step 1: Write the failing test**

`packages/agent-sdk/test/sign.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { recoverMessageAddress } from "viem";
import { scoreAuthMessage } from "@arcade1v1/game-sdk/auth";
import { randomWallet, signScore } from "../src/sign.ts";

test("la firma del score la recupera la wallet del agente", async () => {
  const w = randomWallet();
  const matchId = "0x" + "ab".repeat(32);
  const score = 1234;
  const signature = await signScore({ matchId, address: w.address, score, privateKey: w.privateKey });
  const signer = await recoverMessageAddress({
    message: scoreAuthMessage(matchId, w.address, score),
    signature,
  });
  assert.equal(signer.toLowerCase(), w.address.toLowerCase());
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `node --import tsx --test packages/agent-sdk/test/sign.test.ts`
Expected: FAIL — `../src/sign.ts` no existe.

- [ ] **Step 3: Write the implementation**

`packages/agent-sdk/src/sign.ts`:
```ts
// Wallet efímera + firma del envío de puntaje. En Fase 1 la wallet SOLO firma
// el mensaje del score (auth); no tiene fondos ni hace transacciones on-chain.
import { generatePrivateKey, privateKeyToAccount } from "viem/accounts";
import type { Hex } from "viem";
import { scoreAuthMessage } from "@arcade1v1/game-sdk/auth";

export function randomWallet(): { privateKey: Hex; address: Hex } {
  const privateKey = generatePrivateKey();
  const address = privateKeyToAccount(privateKey).address;
  return { privateKey, address };
}

export async function signScore(opts: {
  matchId: string;
  address: string;
  score: number;
  privateKey: Hex;
}): Promise<Hex> {
  const account = privateKeyToAccount(opts.privateKey);
  return account.signMessage({
    message: scoreAuthMessage(opts.matchId, opts.address, opts.score),
  });
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `node --import tsx --test packages/agent-sdk/test/sign.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/agent-sdk/src/sign.ts packages/agent-sdk/test/sign.test.ts
git commit -m "feat(agent-sdk): firma del envío + wallet efímera"
```

---

### Task 3: Estrategia de 2048 (replay verificable)

**Files:**
- Create: `packages/agent-sdk/src/strategies.ts`
- Test: `packages/agent-sdk/test/strategies.test.ts`

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces:
  - `type PlayResult = { score: number; replay: unknown }`
  - `type Strategy = (seed: number) => PlayResult`
  - `function strategy2048(seed: number, priority?: Dir[]): PlayResult` (`Dir` de `@arcade1v1/game-sdk/g2048`)
  - `const DEFAULT_STRATEGIES: Record<string, Strategy>` (clave `"2048"`)

- [ ] **Step 1: Write the failing test**

`packages/agent-sdk/test/strategies.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { verify2048, type Replay2048 } from "@arcade1v1/game-sdk/g2048";
import { strategy2048 } from "../src/strategies.ts";

test("strategy2048 produce un replay que el verificador reproduce exacto", () => {
  const seed = 123456;
  const { score, replay } = strategy2048(seed);
  // El árbitro re-juega el replay; el puntaje declarado debe coincidir.
  assert.equal(verify2048(replay as Replay2048), score);
  assert.ok(score > 0, "una corrida normal de 2048 hace puntos");
  assert.equal((replay as Replay2048).seed, seed, "el replay usa la semilla de la partida");
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `node --import tsx --test packages/agent-sdk/test/strategies.test.ts`
Expected: FAIL — `../src/strategies.ts` no existe.

- [ ] **Step 3: Write the implementation**

`packages/agent-sdk/src/strategies.ts`:
```ts
// Estrategias de ejemplo: dada la semilla de la partida, juegan headless y
// devuelven el replay (semilla + inputs) listo para enviar al árbitro.
import { Game2048, type Dir } from "@arcade1v1/game-sdk/g2048";

export type PlayResult = { score: number; replay: unknown };
export type Strategy = (seed: number) => PlayResult;

export function strategy2048(
  seed: number,
  priority: Dir[] = ["down", "left", "right", "up"],
): PlayResult {
  const g = new Game2048(seed);
  const moves: Dir[] = [];
  let guard = 0;
  while (!g.over && guard < 5000) {
    let moved = false;
    for (const d of priority) {
      if (g.move(d)) {
        moves.push(d);
        moved = true;
        break;
      }
    }
    if (!moved) break;
    guard++;
  }
  return { score: g.score, replay: { seed, moves } };
}

export const DEFAULT_STRATEGIES: Record<string, Strategy> = {
  "2048": (seed) => strategy2048(seed),
};
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `node --import tsx --test packages/agent-sdk/test/strategies.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/agent-sdk/src/strategies.ts packages/agent-sdk/test/strategies.test.ts
git commit -m "feat(agent-sdk): estrategia de 2048 (replay verificable)"
```

---

### Task 4: createAgent + playAndSubmit

**Files:**
- Create: `packages/agent-sdk/src/agent.ts`
- Modify: `packages/agent-sdk/src/index.ts` (exportar lo nuevo)
- Test: `packages/agent-sdk/test/agent.test.ts`

**Interfaces:**
- Consumes:
  - `ArbiterClient`, `MatchView` (Task 1)
  - `randomWallet`, `signScore` (Task 2)
  - `Strategy`, `DEFAULT_STRATEGIES` (Task 3)
- Produces:
  - `function createAgent(opts: { arbiterUrl?: string; privateKey?: Hex; client?: ArbiterClient }): { address: Hex; client: ArbiterClient; playAndSubmit(args: { game: string; stake: number; strategy?: Strategy }): Promise<MatchView> }`

- [ ] **Step 1: Write the failing test**

`packages/agent-sdk/test/agent.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { recoverMessageAddress } from "viem";
import { scoreAuthMessage } from "@arcade1v1/game-sdk/auth";
import { verify2048, type Replay2048 } from "@arcade1v1/game-sdk/g2048";
import { ArbiterClient } from "../src/client.ts";
import { createAgent } from "../src/agent.ts";

// ArbiterClient falso: devuelve una semilla fija al emparejar y captura el envío.
class FakeArbiter extends ArbiterClient {
  public submitted?: { id: string; address: string; score: number; replay: unknown; signature?: string };
  constructor() {
    super("http://fake");
  }
  async matchmake(game: string, stake: number, address: string) {
    return { matchId: "0x" + "cd".repeat(32), game, stake, seed: 777, status: "waiting", scores: {} } as any;
  }
  async submitScore(id: string, address: string, score: number, replay?: unknown, signature?: string) {
    this.submitted = { id, address, score, replay, signature };
    return { matchId: id, game: "2048", stake: 5, seed: 777, status: "settled", scores: { [address]: score } } as any;
  }
}

test("playAndSubmit juega la semilla de la partida, firma y envía un score verificable", async () => {
  const fake = new FakeArbiter();
  const agent = createAgent({ client: fake });
  await agent.playAndSubmit({ game: "2048", stake: 5 });

  assert.ok(fake.submitted, "se llamó submitScore");
  const s = fake.submitted!;
  // El replay enviado usa la semilla 777 y reproduce el score declarado.
  assert.equal((s.replay as Replay2048).seed, 777);
  assert.equal(verify2048(s.replay as Replay2048), s.score);
  // La firma corresponde a la wallet del agente.
  const signer = await recoverMessageAddress({
    message: scoreAuthMessage(s.id, agent.address, s.score),
    signature: s.signature as `0x${string}`,
  });
  assert.equal(signer.toLowerCase(), agent.address.toLowerCase());
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `node --import tsx --test packages/agent-sdk/test/agent.test.ts`
Expected: FAIL — `../src/agent.ts` no existe.

- [ ] **Step 3: Write the implementation**

`packages/agent-sdk/src/agent.ts`:
```ts
// Helpers de alto nivel: crear un agente y "jugá y enviá" en una sola llamada.
import { privateKeyToAccount } from "viem/accounts";
import type { Hex } from "viem";
import { ArbiterClient, type MatchView } from "./client.js";
import { randomWallet, signScore } from "./sign.js";
import { DEFAULT_STRATEGIES, type Strategy } from "./strategies.js";

export function createAgent(opts: {
  arbiterUrl?: string;
  privateKey?: Hex;
  client?: ArbiterClient;
}): {
  address: Hex;
  client: ArbiterClient;
  playAndSubmit(args: { game: string; stake: number; strategy?: Strategy }): Promise<MatchView>;
} {
  const wallet = opts.privateKey
    ? { privateKey: opts.privateKey, address: privateKeyToAccount(opts.privateKey).address }
    : randomWallet();
  const client = opts.client ?? new ArbiterClient(opts.arbiterUrl ?? "http://localhost:4000");

  async function playAndSubmit(args: { game: string; stake: number; strategy?: Strategy }): Promise<MatchView> {
    const m = await client.matchmake(args.game, args.stake, wallet.address);
    const strat = args.strategy ?? DEFAULT_STRATEGIES[args.game];
    if (!strat) throw new Error(`no hay estrategia por defecto para el juego: ${args.game}`);
    const { score, replay } = strat(m.seed);
    const signature = await signScore({ matchId: m.matchId, address: wallet.address, score, privateKey: wallet.privateKey });
    return client.submitScore(m.matchId, wallet.address, score, replay, signature);
  }

  return { address: wallet.address, client, playAndSubmit };
}
```

Update `packages/agent-sdk/src/index.ts`:
```ts
export { ArbiterClient } from "./client.js";
export type { MatchView, LeaderRow } from "./client.js";
export { createAgent } from "./agent.js";
export { strategy2048, DEFAULT_STRATEGIES } from "./strategies.js";
export type { Strategy, PlayResult } from "./strategies.js";
export { randomWallet, signScore } from "./sign.js";
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `node --import tsx --test packages/agent-sdk/test/agent.test.ts`
Expected: PASS.

- [ ] **Step 5: Run the full SDK test suite + typecheck**

Run: `node --import tsx --test packages/agent-sdk/test/*.test.ts`
Expected: PASS — los 4 archivos de test verdes.

Run: `npx tsc --noEmit -p packages/agent-sdk/tsconfig.json`
Expected: sin errores de tipo.

- [ ] **Step 6: Commit**

```bash
git add packages/agent-sdk/src/agent.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/agent.test.ts
git commit -m "feat(agent-sdk): createAgent + playAndSubmit"
```

---

### Task 5: Ejemplo ejecutable (agente mínimo)

**Files:**
- Create: `packages/agent-sdk/examples/play-2048.ts`
- Modify: `packages/agent-sdk/package.json` (script `example`)

**Interfaces:**
- Consumes: `createAgent` (Task 4).

- [ ] **Step 1: Write the example**

`packages/agent-sdk/examples/play-2048.ts`:
```ts
// Agente mínimo: empareja, juega 2048 y muestra el resultado. Necesita el árbitro
// corriendo (local o publicado). Correr con: ARBITER_URL=... npm run example -w @arcade1v1/agent-sdk
import { createAgent } from "../src/index.js";

const arbiterUrl = process.env.ARBITER_URL ?? "http://localhost:4000";

async function main() {
  const agent = createAgent({ arbiterUrl });
  console.log("Agente:", agent.address);
  const res = await agent.playAndSubmit({ game: "2048", stake: 5 });
  console.log("Estado:", res.status, "· tu score:", res.scores[agent.address]);
  if (res.status === "settled") {
    console.log("Resultado:", res.winner === agent.address ? "GANASTE" : "perdiste",
      "· tu score:", res.yourScore, "· rival:", res.rivalScore);
  } else {
    console.log("Esperando rival. Volvé a consultar el match", res.matchId, "más tarde.");
  }
}

main().catch((e) => {
  console.error("Error:", (e as Error).message);
  console.error("¿Está corriendo el árbitro en", arbiterUrl, "?");
  process.exit(1);
});
```

- [ ] **Step 2: Add the example script**

In `packages/agent-sdk/package.json`, add to a `"scripts"` block (create the block if absent):
```json
  "scripts": {
    "example": "tsx examples/play-2048.ts"
  },
```

- [ ] **Step 3: Smoke-run the example against a local arbiter**

In one terminal: `npm run start -w @arcade1v1/server` (needs `apps/server/.env` with `ARBITER_PRIVATE_KEY`).
In another: `npm run example -w @arcade1v1/agent-sdk`
Expected: imprime la dirección del agente y el estado del match (`waiting` si no hay rival, o `settled` si lo hay vía un segundo agente). No debe tirar excepción.

> Si no se puede levantar el árbitro en el entorno, marcar este step como verificado por revisión de código del ejemplo (imports válidos, `tsc` limpio) y dejar nota.

- [ ] **Step 4: Typecheck**

Run: `npx tsc --noEmit -p packages/agent-sdk/tsconfig.json`
Expected: sin errores.

- [ ] **Step 5: Commit**

```bash
git add packages/agent-sdk/examples/play-2048.ts packages/agent-sdk/package.json
git commit -m "feat(agent-sdk): ejemplo ejecutable play-2048"
```

---

### Task 6: Migrar la web al cliente del SDK (DRY)

**Files:**
- Modify: `apps/web/app/lib/arbiter.ts` (usar `ArbiterClient` del SDK por dentro, manteniendo la API que la web ya consume)
- Modify: `apps/web/package.json` (agregar dependencia `@arcade1v1/agent-sdk`)

**Interfaces:**
- Consumes: `ArbiterClient`, `MatchView` (Task 1).
- Preserva (la web sigue importando esto sin cambios): `matchmake`, `submitScore`, `getMatch`, `getLeaderboard`, `playerId`, y el tipo `MatchView`.

- [ ] **Step 1: Add the dependency**

In `apps/web/package.json`, under `"dependencies"`, add:
```json
    "@arcade1v1/agent-sdk": "*",
```
Run: `npm install`
Expected: linkea el workspace sin errores.

- [ ] **Step 2: Rewrite arbiter.ts to delegate to the SDK (keep the same exports)**

Replace the body of `apps/web/app/lib/arbiter.ts` with:
```ts
// Cliente del backend "arbitro" para la web. Delega en @arcade1v1/agent-sdk
// (cliente canónico) y conserva los helpers propios de la web (playerId).
import { ArbiterClient, type MatchView } from "@arcade1v1/agent-sdk";

export type { MatchView };

const BASE = process.env.NEXT_PUBLIC_ARBITER_URL || "http://localhost:4000";
const client = new ArbiterClient(BASE);

export function matchmake(game: string, stake: number, address: string) {
  return client.matchmake(game, stake, address);
}

export function submitScore(
  id: string,
  address: string,
  score: number,
  replay?: unknown,
  signature?: string,
) {
  return client.submitScore(id, address, score, replay, signature);
}

export function getMatch(id: string, address?: string) {
  return client.getMatch(id, address);
}

export interface LeaderRow {
  address: string;
  rating: number;
}

export async function getLeaderboard(game: string, limit = 20): Promise<LeaderRow[]> {
  try {
    return await client.leaderboard(game, limit);
  } catch {
    return [];
  }
}

/** Identificador del jugador: wallet si esta conectada, o un "invitado" local. */
export function playerId(walletAddress: string | null): string {
  if (walletAddress) return walletAddress;
  let g = localStorage.getItem("arcade.guest");
  if (!g) {
    const hex = "0123456789abcdef";
    g = "0x" + Array.from({ length: 40 }, () => hex[Math.floor(Math.random() * 16)]).join("");
    localStorage.setItem("arcade.guest", g);
  }
  return g;
}
```

- [ ] **Step 3: Verify the web still typechecks and builds**

Run: `npm run typecheck:web`
Expected: sin errores.

Run: `npm run build -w @arcade1v1/web` (o `cd apps/web && next build` si no existe el script)
Expected: build exitoso (la web sigue compilando con el cliente delegado).

> Nota para el revisor: confirmar que `MatchView` del SDK incluye todos los campos que la web usa (`yourScore`, `rivalScore`, `margin`, `netPnl`, `rivalReplay`, `rating`, `ratingDelta`, `signature`, `winner`, `outcome`, `role`, `opponent`, `isBot`?). Si la web usa `isBot`, agregarlo a `MatchView` en `client.ts` (Task 1) — `isBot?: boolean`.

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/lib/arbiter.ts apps/web/package.json package-lock.json
git commit -m "refactor(web): usar ArbiterClient de @arcade1v1/agent-sdk (DRY)"
```

---

## Self-Review

- **Spec coverage:**
  - `ArbiterClient` portable → Task 1. ✓
  - Re-export motores / jugar headless → via `strategies.ts` (Task 3) que usa `game-sdk`. ✓
  - Helpers `createAgent` / `playAndSubmit` → Task 4. ✓
  - Wallet efímera + firma → Task 2. ✓
  - Estrategias de ejemplo → Task 3. ✓
  - Plantilla/ejemplo → Task 5. ✓
  - Extraer cliente de la web y que la web lo importe → Task 6. ✓
  - Sin cambios en contrato/árbitro → ninguna task toca `packages/contracts` ni `apps/server`. ✓
  - Tests (mock fetch + integración con fake client) → Tasks 1 y 4. ✓
- **Placeholder scan:** sin TBD/TODO; el único condicional es el smoke-run de Task 5 (levantar el árbitro) con su fallback explícito de revisión por código.
- **Type consistency:** `MatchView` definido en Task 1 y reusado en Tasks 4 y 6; `Strategy`/`PlayResult` definidos en Task 3 y consumidos en Task 4; `randomWallet`/`signScore` (Task 2) consumidos en Task 4; `isBot?` señalado en Task 6 por si la web lo necesita.

## Notas para el segundo plan (fuera de este)

`@arcade1v1/mcp` (servidor MCP) se construirá en un plan aparte una vez que este
SDK esté hecho: dependerá de `@arcade1v1/agent-sdk` y expondrá las tools
`list_games`, `matchmake`, `play_and_submit`, `get_result`, `leaderboard`, `rating`.
